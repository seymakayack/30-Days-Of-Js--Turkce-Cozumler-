//!console.log(text.replace(/[.,]/g, "")); string'deki noktalama işaretlerini uzaklaştırma.
/* let arr=Array(0);
let arr1=[];
console.log(arr);
console.log(arr1);

let arr2=[1,2,3];
console.log(arr2.length);
console.log(arr2[0]);
console.log(arr2[1]);
console.log(arr2[2]);

let companies=['Google', 'Facebook', 'IBM', 'Microsoft', 'Apple', 'Oracle', 'Amazon'];
console.log(companies[0].toUpperCase());
console.log(companies.toString());
if(companies.includes('IBM')){
    console.log("IBM")
}
else{
    console.log("Company is not found");
}

let firstIndex=companies[0].match(/o/g);
let secondIndex= companies[1].match(/o/g);
let lastIndex=companies[2].match(/o/g);
console.log(firstIndex);
console.log(secondIndex);
console.log(lastIndex);

console.log(companies.slice(2));
console.log(companies.slice(4,7))
console.log(companies.slice(3,4));
console.log(companies.splice(0,1));
console.log(companies.splice(2,1));
console.log(companies.splice()); 

let text ='I love teaching and empowering people. I teach HTML, CSS, JS, React, Python.'
console.log(text.replace(/[.,]/g, ""));
let text1=text.split(' ')
console.log(text1.length);
const shoppingCart = ['Milk', 'Coffee', 'Tea', 'Honey']
if(!shoppingCart.includes('Meat')){
    shoppingCart.unshift('Meat');
}

if(!shoppingCart.includes('Sugar')){
    shoppingCart.push('Sugar');
}

if(shoppingCart.includes('Honey')){
    shoppingCart.splice(shoppingCart.indexOf('Honey'),1);
}

shoppingCart[shoppingCart.indexOf('Tea')]='Green Tea';
console.log(shoppingCart);

const countries = [
    'Albania',
    'Bolivia',
    'Canada',
    'Denmark',
    'Ethiopia',
    'Finland',
    'Germany',
    'Hungary',
    'Ireland',
    'Japan',
    
  ];
  if(countries.includes('Ethiopia')){
    console.log(countries[countries.indexOf('Ethiopia')].toUpperCase());
  }
  else{
    console.log(countries);
  }

const frontEnd = ['HTML', 'CSS', 'JS', 'React', 'Redux']
const backEnd = ['Node','Express', 'MongoDB']
let fullStack=frontEnd.concat(backEnd);

console.log(fullStack)

const ages = [19, 22, 19, 24, 20, 25, 26, 24, 25, 24];
ages.sort();
console.log(ages);
let maxAge=ages[ages.length-1];
let minAge=ages[0];
let medianAge=ages[((ages.length)/2)-1];
console.log(`Max age is ${ages[ages.length-1]}`)
console.log(`Min age is ${ages[0]}`);
console.log(`Median age is ${ages[(ages.length/2)-1]}`);
console.log(Math.abs(minAge-medianAge));
console.log(Math.abs(maxAge-medianAge));

console.log(countries.slice(0,10));
console.log(`${countries[(ages.length/2)]}`);
let newArr= countries.slice(5);
console.log(newArr);
let newArr2=countries.slice(0,5);
console.log(newArr2);*/





















